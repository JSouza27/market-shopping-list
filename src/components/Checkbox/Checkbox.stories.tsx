import { Meta, StoryObj } from '@storybook/react';
import Checkbox from '.';

export default {
  title: 'Checkbox',
  component: Checkbox,
  parameters: {
    layout: 'centered'
  }
} as Meta;

export const Default: StoryObj = {};
